# Notion integration for the consultation form (/api/consultation.ts)
# Copy to .env.local for local dev, and set the same vars in Vercel project settings.

# Internal integration secret from https://www.notion.so/my-integrations
NOTION_TOKEN=

# The target database id (the 32-char id in the database URL)
NOTION_DATABASE_ID=
